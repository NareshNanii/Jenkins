version: '3.8'

services:
  jenkins:
    build:
      context: .
      dockerfile: dockerfile.jenkins
    container_name: jenkins
    user: root
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - /c/Users/Monisha Loganathan/.kube/config:/root/.kube/config


    restart: unless-stopped

volumes:
  jenkins_home:
